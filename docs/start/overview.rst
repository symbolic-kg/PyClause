
Overview
========

This will be an example where we just create some triples within code.
Define some rules (maybe Anna speaks english) in code

Load data and rules and then do some stuff (mention that also paths can be specified).

